// ==========================================
// RISPOSTE AI TEST
// ==========================================

// ------------------------------------------
// 🟢 TEST SUPERATI PERFETTAMENTE
// ------------------------------------------
// In questi casi il programma si comporta molto bene, senza errori logici o di calcolo.

// ----test_circuito_aperto:
// Perfetto.

// ----test_pot_nullo:
// Superato bene. L'algoritmo gestisce il sistema omogeneo (0V) senza problemi di pivoting.

// ----test_res_nulla:
// Superato bene. Il metodo delle correnti di maglia non soffre della divisione per zero con R=0.

// ----test_riga_vuota:
// Superato bene. Il parser ignora correttamente le righe vuote senza interrompere la lettura.

// ----test_val_int:
// Superato bene. Il cast implicito da interi a double funziona e non perde precisione decimale.


// ------------------------------------------
// 🟡 TEST PASSATI... MA CON PROBLEMI (Fallimenti Silenziosi / Errori Logici)
// ------------------------------------------
// ----Il programma non crasha e arriva alla fine, ma la logica matematica, la fisica o la 
// gestione dell'input presentano dei problemi o dei comportamenti da valutare/correggere.

// ----test_arco_cappio:
// Il programma gira lo stesso e risponde correttamente. Bisogna solo valutare se non 
// trattarlo direttamente come un errore, essendo privo di senso logico in questo contesto.

// ----test_comp_duplicate:
// Il programma non ha sovrascritto nulla, tuttavia ha mantenuto lo stesso nome per i componenti. 
// Si deve valutare se tenere la situazione così o gestirla come un errore, restituendo 
// "componenti duplicate" o roba simile.

// ---- test_componente_estranea:
// Ha completamente ignorato la componente estranea "H" nei conti; tuttavia bisognerebbe 
// mettere un blocco di errore perché poi questo componente finisce comunque nella lista 
// iniziale dei componenti stampati.

// ----test_doppia_res:
// Il programma non crasha però fa pasticcio: nell'output spariscono le due componenti 
// in parallelo e restituisce calcoli errati. Inoltre, due resistenze in parallelo dovrebbero 
// avere la STESSA TENSIONE (V) ai loro capi ma qua risultano diverse. Bisogna risolvere 
// inserendo un controllo che verifichi se l'arco collegante il nodo 1 e il nodo 2 è già occupato.

// ----test_doppio_gen:
// Il programma ignora completamente uno dei due generatori (l'ultimo letto sovrascrive il primo). 
// Dovrebbe invece segnalare un errore, perché dal punto di vista fisico avere due generatori 
// diversi in parallelo sullo stesso ramo è un paradosso impossibile.

// ----test_formato_errato2:
// Il programma non crasha e ignora completamente le componenti che hanno dei nodi espressi 
// con delle lettere (NON numeri). Bisognerebbe inserire una segnalazione di errore nel parser.

// ----test_mancanza_di_nodi:
// Il programma non crasha; ignora completamente le righe in cui manca almeno un nodo e 
// calcola tutto il resto (l'utente riceve dati parziali senza saperlo). Bisogna restituire un errore.

// ----test_res_negativa:
// Il programma non crasha e tratta tutto matematicamente in modo perfetto (Ohm applicato al segno meno). 
// Tuttavia, dal punto di vista fisico le resistenze negative passive non esistono, andrebbe messo un blocco.


// ------------------------------------------
// 🔴 TEST CON CRASH BRUTALE
// ------------------------------------------
// In questi casi il codice va in panico a causa di dati assenti o corrotti che mandano 
// in errore il motore di calcolo topologico.

// ----test_file_vuoto:
// Crasha nella fase di calcolo delle maglie (stesso errore di test_doppio_circ) perché 
// riceve un grafo vuoto con 0 nodi e 0 rami senza che il parser si sia accorto del file vuoto.

// ----test_file_con_stringa:
// Crasha nella fase di calcolo delle maglie (stesso errore di test_doppio_circ) perché 
// la stringa manda in errore lo stream di lettura, passando dati corrotti o incompleti all'algoritmo.

// ----test_doppio_circ:
// Il programma legge tutto ma poi crasha brutalmente a metà strada nella fase dei grafi, 
// lanciando questa eccezione di runtime perché l'algoritmo non gestisce grafi disconnessi (due circuiti isolati):
// terminate called after throwing an instance of 'std::runtime_error'
// what(): Errore: il grafo C non ha dimensione k, come dovrebbe
// Aborted